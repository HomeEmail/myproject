//全局的aciton

import * as types from './mutation-types'

export const getSomeData = ({commit},payload) => {

};



/*export const addToCart = ({ commit }, product) => {
	if (product.inventory > 0) {
		commit(types.get_actived_pm, {
			id: product.id
		});
	}
}*/